<div class="w-full max-w-2xl mx-auto">
  <h1 class="text-3xl font-bold mb-2"><%= @mini_app.name %> — Leaderboard</h1>
  <p class="text-gray-500 mb-8">Top scores</p>

  <%= render LeaderboardComponent.new(app_slug: @mini_app.slug) %>
</div>
